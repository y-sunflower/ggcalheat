% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/geom-calendar-interactive.R
\name{geom_calendar_interactive}
\alias{geom_calendar_interactive}
\title{Interactive calendar heatmap geometry for ggiraph}
\usage{
geom_calendar_interactive(
  mapping = NULL,
  data = NULL,
  ...,
  week_start = "sunday",
  start_date = NULL,
  end_date = NULL,
  na_value = 0,
  cell_width = 0.95,
  cell_height = 0.95,
  show_month_labels = TRUE,
  month_labels = "\%b",
  month_label_y = 0.35,
  month_label_size = 3.5,
  month_label_color = "grey20",
  month_label_vjust = 1,
  show_day_labels = TRUE,
  day_labels = NULL,
  square = TRUE,
  color = NA,
  linewidth = 0,
  na.rm = FALSE,
  show.legend = NA,
  inherit.aes = TRUE
)
}
\arguments{
\item{mapping}{Set of aesthetic mappings created by \code{\link[ggplot2:aes]{ggplot2::aes()}}. Must
include \code{date} and \code{value}.}

\item{data}{A data frame.}

\item{...}{Additional parameters passed on to \code{\link[ggplot2:layer]{ggplot2::layer()}}.}

\item{week_start}{First day of week. One of \code{"sunday"}, \code{"monday"}, \code{0}, or
\code{1}.}

\item{start_date}{Optional lower date bound for the displayed range.}

\item{end_date}{Optional upper date bound for the displayed range.}

\item{na_value}{Value assigned to dates that are missing from input data.}

\item{cell_width}{Width of each day tile.}

\item{cell_height}{Height of each day tile.}

\item{show_month_labels}{If \code{TRUE}, draws month labels below the calendar
using abbreviated names by default.}

\item{month_labels}{Month label formatter. Use a single date format string
(default \code{"\%b"}) or a function that takes month-start dates and returns
labels.}

\item{month_label_y}{Vertical position for month labels (in calendar y units).}

\item{month_label_size}{Text size for month labels.}

\item{month_label_color}{Text color for month labels.}

\item{month_label_vjust}{Vertical justification for month labels.}

\item{show_day_labels}{If \code{TRUE}, shows weekday labels on the y-axis.}

\item{day_labels}{Optional custom weekday labels (character vector of length
7, top to bottom).}

\item{square}{If \code{TRUE}, adds \code{\link[ggplot2:coord_fixed]{ggplot2::coord_fixed()}} (\code{ratio = 1}) so day
cells render as squares by default.}

\item{color}{Tile border color.}

\item{linewidth}{Tile border line width.}

\item{na.rm}{Passed to \code{\link[ggplot2:geom_tile]{ggplot2::geom_tile()}} to control NA handling.}

\item{show.legend}{Should this layer be included in legends?}

\item{inherit.aes}{If \code{FALSE}, overrides global aesthetics.}
}
\value{
A ggplot2 component using ggiraph interactive geometry. By default
this includes calendar tiles, hidden x ticks, weekday labels on y, month
labels below the heatmap, and \code{\link[ggplot2:coord_fixed]{ggplot2::coord_fixed()}} so day tiles stay
square.
}
\description{
\code{geom_calendar_interactive()} behaves like \code{\link[=geom_calendar]{geom_calendar()}} but draws tiles
with ggiraph's interactive geometry.
}
\details{
By default, each day has:
\itemize{
\item \code{data_id = as.character(date)}
\item \code{tooltip = paste0(date, ": ", value)}
}

You can override these by mapping interactive aesthetics (\code{tooltip},
\code{data_id}, \code{onclick}, etc.) inside \code{\link[ggplot2:aes]{ggplot2::aes()}}.
}
\examples{
\dontshow{if (requireNamespace("ggiraph", quietly = TRUE)) withAutoprint(\{ # examplesIf}
library(ggplot2)
library(ggiraph)

set.seed(42)
daily <- data.frame(
  date = seq.Date(as.Date("2025-01-01"), as.Date("2025-01-31"), by = "day"),
  value = rpois(31, lambda = 3)
)

p <- ggplot(daily, aes(date = date, value = value)) +
  geom_calendar_interactive(
    aes(
      tooltip = paste("Date:", date, "\nValue:", value),
      data_id = date
    )
  ) +
  scale_fill_gradient(low = "grey95", high = "#2C7FB8")

girafe(ggobj = p)
\dontshow{\}) # examplesIf}
}
